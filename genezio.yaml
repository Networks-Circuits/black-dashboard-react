frontend:
    path: .
    publish: build
    scripts:
        deploy:
            - npm install --legacy-peer-deps
            - npm run build
    subdomain: ISP-manager
name: ISP-manager
region: us-east-1
yamlVersion: 2
